# SafetyCarGoal2-v0 实验报告

## 摘要

本实验在 Safety-Gymnasium `SafetyCarGoal2-v0`（最高难度）环境上，对比了无安全约束的标准 PPO 与引入拉格朗日乘子的安全强化学习算法 PPOLag，使用 OmniSafe 0.5.0 框架完成训练、评估与可视化。两次训练均采用相同骨干超参数（500,000 步），通过奖励、成本及拉格朗日乘子的变化分析安全约束的实际效果。

---

## 1. 环境描述

### 1.1 SafetyCarGoal2-v0

`SafetyCarGoal2-v0` 是 Safety-Gymnasium 中基于 MuJoCo 物理引擎的安全导航任务，具体设置如下：

| 属性 | 描述 |
|---|---|
| **物理引擎** | MuJoCo（刚体动力学） |
| **智能体** | 圆柱形双轮差速小车（Car） |
| **任务目标** | 驾驶至随机生成的目标点（Goal） |
| **危险物** | 随机分布的圆柱形障碍物（Hazards）和脆弱花瓶（Vases） |
| **难度等级** | Level 2（障碍物密度最高，路径最复杂） |
| **回合长度** | 固定 1000 步（TimeLimit） |

Level 2 与较低难度的区别在于障碍物数量更多、目标点距离更远，要求智能体在躲避障碍的同时持续寻路。

### 1.2 观察空间

观察向量共 **72 维**，通过连接以下传感器输出构成：

| 传感器 | 维度 | 含义 |
|---|---|---|
| `accelerometer` | 3 | 线加速度 |
| `velocimeter` | 3 | 线速度 |
| `gyro` | 3 | 角速度 |
| `magnetometer` | 3 | 偏航角（磁罗盘） |
| `ballangvel_rear` | 3 | 后轮角速度 |
| `ballquat_rear` | 9 | 后轮旋转矩阵 |
| `goal_lidar` | 16 | 目标方向扇区 LiDAR（0–1 距离编码） |
| `hazards_lidar` | 16 | 危险物 LiDAR |
| `vases_lidar` | 16 | 花瓶 LiDAR |

其中 LiDAR 传感器将周围 360° 均匀分为 16 个扇区，每个扇区的值表示该方向上最近物体的归一化倒数距离（越大越近）。

### 1.3 动作空间

2 维连续动作 $a \in [-1, 1]^2$，对应左右轮的归一化驱动力矩，由差速驱动模型转换为线速度和角速度。

---

## 2. 奖励函数

### 2.1 任务奖励

每步奖励由三部分构成：

$$
r_t = r_{\text{goal}} + r_{\text{dist}} + r_{\text{ctrl}}
$$

| 分量 | 来源 | 描述 |
|---|---|---|
| $r_{\text{goal}}$ | 到达目标点 | 到达目标时给予 **+1.0** 的稀疏奖励，目标随即重置 |
| $r_{\text{dist}}$ | 距离缩短量 | $\Delta d = d_{t-1} - d_t$，正比于向目标靠近的程度 |
| $r_{\text{ctrl}}$ | 控制惩罚 | 极小的动作幅度惩罚（鼓励平滑控制） |

因此智能体的主要激励是**持续接近并到达目标点**，每次到达后计数加一且目标重置，一回合内可多次到达目标。

### 2.2 安全成本（Cost）

成本是独立于奖励的安全违规信号，不进入 PPO 的优化目标：

$$
c_t = c_{\text{hazard}} + c_{\text{vase\_contact}} + c_{\text{vase\_velocity}}
$$

| 分量 | 触发条件 | 值 |
|---|---|---|
| $c_{\text{hazard}}$ | 小车进入危险物圆形禁区 | 每步 +1 |
| $c_{\text{vase\_contact}}$ | 小车与花瓶发生接触 | 每次 +1 |
| $c_{\text{vase\_velocity}}$ | 高速撞击花瓶 | 正比于碰撞速度 |

一个回合的累计成本 $C = \sum_{t=0}^{T} c_t$ 反映了安全约束的违反程度。**安全强化学习的目标是在保持较高奖励的同时，将 $C$ 控制在阈值 $d$ 以下。**

---

## 3. 状态转移原理

### 3.1 MuJoCo 物理模型

环境底层由 MuJoCo 解算器以恒定时间步长（$\Delta t = 0.002$ s，每步积分 10 次）推进刚体动力学：

$$
s_{t+1} = f_{\text{MuJoCo}}(s_t, a_t)
$$

小车的运动遵循差速驱动模型，控制信号经电机模型转化为轮力矩，再通过摩擦、惯性等物理约束产生连续的六自由度运动。

### 3.2 目标重置机制

每次到达目标点（身体中心与目标点距离 $< 0.3$ m）后，新目标点在场景内随机采样，保证其不与障碍物重叠。这使得智能体需要在整个回合内持续导航。

### 3.3 回合终止条件

| 条件 | 类型 |
|---|---|
| 步数达到 1000 步 | `truncated = True` |
| 控制器异常（极少发生） | `terminated = True` |

本环境无因碰撞而提前终止的机制，碰撞仅产生成本，不结束回合。

---

## 4. 两种策略的区别

### 4.1 标准 PPO（无安全约束）

PPO 的优化目标仅包含任务奖励：

$$
\max_\theta \; \mathbb{E}_t \left[ \min\!\left( \rho_t(\theta)\, \hat{A}_t,\;\; \text{clip}(\rho_t(\theta), 1-\varepsilon, 1+\varepsilon)\, \hat{A}_t \right) \right]
$$

其中 $\rho_t(\theta) = \pi_\theta(a_t|s_t) / \pi_{\theta_\text{old}}(a_t|s_t)$ 为重要性采样比，$\hat{A}_t$ 为 GAE 优势估计，$\varepsilon = 0.2$ 为裁剪范围。

**关键特征：**
- 完全不感知成本信号（`use_cost = False`）
- 仅有一个 Critic 估计奖励价值函数
- 策略自由地最大化奖励，不受任何安全限制

### 4.2 PPOLag（拉格朗日安全约束）

PPOLag 将安全约束转化为带惩罚的拉格朗日问题：

$$
\max_\theta \min_{\lambda \ge 0} \; J_R(\theta) - \lambda \left( J_C(\theta) - d \right)
$$

其中 $J_R(\theta) = \mathbb{E}[\sum \gamma^t r_t]$ 为期望累计奖励，$J_C(\theta) = \mathbb{E}[\sum \gamma^t c_t]$ 为期望累计成本，$d = 25$ 为成本阈值，$\lambda \ge 0$ 为拉格朗日乘子。

**实际训练目标**替换为带安全惩罚的 PPO 截断目标：

$$
\max_\theta \; \mathbb{E}_t \left[ L_t^{\text{CLIP}}(\theta) - \lambda \cdot L_t^{\text{COST}}(\theta) \right]
$$

**拉格朗日乘子的自适应更新：**

$$
\lambda \leftarrow \lambda + \alpha_\lambda \cdot (J_C(\theta) - d)
$$

- 若实际成本 $J_C > d$（超出约束），$\lambda$ 增大 → 成本惩罚加重 → 策略趋于保守
- 若实际成本 $J_C < d$（满足约束），$\lambda$ 减小 → 成本惩罚减轻 → 策略逐渐追求更高奖励

**关键特征：**
- 额外增加一个成本 Critic 估计安全价值函数
- 拉格朗日乘子 $\lambda$ 作为动态权重在训练中自适应调节
- 以牺牲部分奖励换取对安全成本的主动控制

### 4.3 超参数对照

| 超参数 | PPO | PPOLag |
|---|---|---|
| 学习率 | 3×10⁻⁴ | 3×10⁻⁴ |
| 折扣因子 $\gamma$ | 0.99 | 0.99 |
| GAE $\lambda$ | 0.95 | 0.95 |
| PPO 裁剪 $\varepsilon$ | 0.2 | 0.2 |
| 每 epoch 步数 | 5,000 | 5,000 |
| 批量大小 | 256 | 256 |
| 网络结构 | MLP [64, 64] Tanh | MLP [64, 64] Tanh |
| 成本限制 $d$ | — | 25.0 |
| 乘子初始值 $\lambda_0$ | — | 0.001 |
| 乘子学习率 $\alpha_\lambda$ | — | 0.035 |
| 乘子优化器 | — | Adam |
| `use_cost` | False | True |

---

## 5. 实验结果

### 5.1 训练设置

| 设置 | 值 |
|---|---|
| 环境 | SafetyCarGoal2-v0 (Level 2) |
| 随机种子 | 42 |
| 总训练步数 | 500,000（100 epochs × 5,000 steps） |
| 硬件 | CPU（Intel，单线程） |
| 框架 | OmniSafe 0.5.0 |

### 5.2 训练过程统计

|  | PPO | PPOLag |
|---|---|---|
| 训练时长 | 76 分钟 | 83 分钟 |
| **奖励均值** | **14.10** | 1.45 |
| 奖励标准差 | 5.94 | 0.73 |
| 奖励最大值 | 20.31 | 3.40 |
| **成本均值** | 198.66 | **86.18** |
| 成本标准差 | 40.93 | 19.83 |
| 成本最小值 | 46.20 | 37.70 |
| **最终 epoch 奖励** | **19.33** | 1.15 |
| **最终 epoch 成本** | 238.92 | **54.52** |
| 拉格朗日乘子（最终） | — | 3.59 |

**奖励趋势（前10 epoch → 后10 epoch 均值）：**

| 算法 | 前 10 epoch | 后 10 epoch | 变化 |
|---|---|---|---|
| PPO | 1.32 | 19.86 | +18.54（持续增长） |
| PPOLag | 0.80 | 1.17 | +0.37（受约束抑制） |

**成本趋势（前10 epoch → 后10 epoch 均值）：**

| 算法 | 前 10 epoch | 后 10 epoch | 变化 |
|---|---|---|---|
| PPO | 93.44 | 219.91 | +126.47（随奖励增大而升高） |
| PPOLag | 62.76 | 72.30 | +9.54（在约束下缓慢上升） |

### 5.3 评估结果（epoch-100.pt，测试期不更新策略）

**PPO（10 回合评估）：**

| 回合 | 奖励 | 成本 |
|---|---|---|
| 均值 | 20.50 | 202.2 |
| 标准差 | 4.46 | 137.4 |
| 最高奖励回合 | 28.81 | 43.0 |
| 零成本回合数 | 0 / 10 | — |

**PPOLag（10 回合评估）：**

| 回合 | 奖励 | 成本 |
|---|---|---|
| 均值 | 1.00 | 47.3 |
| 标准差 | 2.94 | 60.1 |
| 最高奖励回合 | 9.12 | 61.0 |
| **零成本回合数** | **5 / 10** | — |

---

## 6. 结论与讨论

### 6.1 核心发现

1. **PPO 奖励优势明显**：标准 PPO 在 500k 步内奖励从 ~1 增长至 ~20，充分利用了无约束条件下的策略空间，但成本同步飙升至 ~240/回合（约为安全阈值 25 的 10 倍）。

2. **PPOLag 成本降低 76%**：PPOLag 将回合均成本从 202 压降至 47（训练均值从 199 降至 86），**拉格朗日乘子从 0.036 升至 3.59**，表明安全惩罚在训练中持续加强。

3. **零成本回合出现**：评估阶段 PPOLag 有 5/10 回合成本为 0，说明智能体已在部分场景中学会完全避开障碍物，而 PPO 的所有评估回合均有成本产生。

4. **100 epochs 不足以完全收敛安全约束**：PPOLag 最终成本 54.5 仍高于目标 25，Lagrange 乘子仍在上升，预计需要 **300–500 epochs** 才能使成本稳定在约束下方，届时奖励也有望恢复增长。

### 6.2 奖励–安全权衡分析

| 指标 | PPO | PPOLag | PPOLag 相对变化 |
|---|---|---|---|
| 评估奖励均值 | 20.50 | 1.00 | **−95%** |
| 评估成本均值 | 202.2 | 47.3 | **−77%** |
| 零成本比例 | 0% | 50% | **+50pp** |

PPOLag 以显著的奖励代价换取了成本的大幅削减，符合安全 RL 中的奖励–安全 Pareto 权衡规律。要进一步平衡两者，可考虑：
- 延长训练步数（≥ 1M）
- 提高 $d$（放宽约束，如 $d = 50$）
- 换用 CPO 或 FOCOPS 等更高效的约束优化算法

---

*实验记录：2026-04-03 | OmniSafe 0.5.0 | SafetyCarGoal2-v0 | seed=42*
